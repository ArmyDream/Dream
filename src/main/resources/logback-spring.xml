<?xml version="1.0" encoding="UTF-8" ?>
<configuration>
    <!-- 属性文件:在properties文件中找到对应的配置项 -->
    <springProperty scope="context" name="logging.name" source="spring.application.name" defaultValue="dream-log"/>
    <springProperty scope="context" name="logging.path" source="logging.file.path" defaultValue="logs/${logging.name}"/>
    <springProperty scope="context" name="logging.max-history" source="logging.logback.rollingpolicy.max-history"
                    defaultValue="15"/>
    <springProperty scope="context" name="logging.max-size" source="logging.logback.rollingpolicy.max-file-size"
                    defaultValue="300MB"/>

    <contextName>logback</contextName>
    <appender name="consoleLog" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <!--格式化输出（配色）：%d表示日期，%thread表示线程名，%-5level：级别从左显示5个字符宽度%msg：日志消息，%n是换行符-->
            <pattern>%yellow(%d{yyyy-MM-dd HH:mm:ss}) %red([%thread]) %highlight(%-5level) %cyan(%logger{50}) -
                %magenta(%msg) %n
            </pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!--根据日志级别分离日志，分别输出到不同的文件-->
    <appender name="fileInfoLog" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${logging.path}/${logging.name}.log</file>
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>ERROR</level>
            <onMatch>DENY</onMatch>
            <onMismatch>ACCEPT</onMismatch>
        </filter>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>
                %d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{50} - %msg%n
            </pattern>
            <charset>UTF-8</charset>
        </encoder>
        <!--滚动策略-->
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!--按时间保存日志 修改格式可以按小时、按天、月来保存-->
            <fileNamePattern>${logging.path}/${logging.name}.info.%d{yyyy-MM-dd}.log</fileNamePattern>
            <!--保存时长-->
            <MaxHistory>${logging.max-history}</MaxHistory>
            <!--文件大小-->
            <totalSizeCap>${logging.max-size}</totalSizeCap>
            <cleanHistoryOnStart>true</cleanHistoryOnStart>
        </rollingPolicy>
    </appender>

    <appender name="fileErrorLog" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>ERROR</level>
        </filter>
        <encoder>
            <pattern>
                %d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{50} - %msg%n
            </pattern>
        </encoder>
        <!--滚动策略-->
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!--路径-->
            <fileNamePattern>${logging.path}/${logging.name}.error.%d{yyyy-MM-dd}.log</fileNamePattern>
            <MaxHistory>${logging.max-history}</MaxHistory>
            <!--文件大小-->
            <totalSizeCap>${logging.max-size}</totalSizeCap>
            <cleanHistoryOnStart>true</cleanHistoryOnStart>
        </rollingPolicy>
    </appender>

    <!--配置多环境日志输出  可以在application.properties中配置选择哪个profiles : spring.profiles.active=dev-->
    <!--开发环境:打印控制台-->
    <springProfile name="dev,test,sit,pre,fat,staging">
        <root level="info">
            <appender-ref ref="consoleLog"/>
            <appender-ref ref="fileInfoLog"/>
            <appender-ref ref="fileErrorLog"/>

            <!-- 可选：针对特定包的日志级别调整 -->
            <logger name="com.dream" level="debug"/>
            <!--            <logger name="org.springframework.web" level="debug"/>-->
            <!--            <logger name="org.mybatis" level="debug"/>-->
        </root>
    </springProfile>

    <!-- 生产环境：（根据你的需要配置） -->
    <springProfile name="prod,production,uat,dr">
        <root level="info">
            <appender-ref ref="fileInfoLog"/>
            <appender-ref ref="fileErrorLog"/>
        </root>
    </springProfile>

    <!-- 默认配置（未匹配到任何 profile 时的兜底） -->
    <root level="info">
        <!--        <appender-ref ref="consoleLog"/>-->
        <appender-ref ref="fileInfoLog"/>
        <appender-ref ref="fileErrorLog"/>

        <!-- 可选：针对特定包的日志级别调整 -->
        <logger name="com.dream" level="debug"/>

    </root>

</configuration>